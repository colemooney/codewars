function solution(a, b){
//assigns long and short based off length using ternary operator
  let long = a.length > b.length ? a : b;
  let short = a.length < b.length ? a : b;
   return short + long + short;
} 
